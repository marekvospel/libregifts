{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "FORCE_COLOR=1 node server",
    "build": "pnpm run build:client && pnpm run build:server",
    "build:client": "FORCE_COLOR=1 vite build --ssrManifest --outDir dist/client",
    "build:server": "FORCE_COLOR=1 vite build --ssr src/entry-server.ts --outDir dist/server",
    "preview": "FORCE_COLOR=1 NODE_ENV=production node server"
  },
  "dependencies": {
    "@discordjs/collection": "^0.5.0",
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "pinia": "^2.0.12",
    "serve-static": "^1.14.2",
    "vue": "^3.2.25",
    "vue-router": "4"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@vitejs/plugin-vue": "^2.2.0",
    "@vue/eslint-config-typescript": "^10.0.0",
    "eslint": "^8.11.0",
    "eslint-plugin-vue": "^8.5.0",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "vite-plugin-pages": "^0.22.0"
  }
}